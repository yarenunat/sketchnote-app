// Cursor TODO (post-v1, not required for initial build):
// Placeholder for future cross-device sync (iCloud / Google Drive / custom
// backend). Do NOT implement this until the core drawing experience,
// storage, and export are solid — sync adds significant complexity
// (conflict resolution for concurrent edits, large binary stroke data
// transfer) and is easy to bolt on later if the local storage layer
// (storage_service.dart) has clean boundaries.
